import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'zamedia.de',
      'tasolutions.io',
      'media.licdn.com',
      'madcomdigital.com',
      'www.erlystagestudios.com',
      'mountpixels.com',
    ],
  },
};

export default nextConfig;
